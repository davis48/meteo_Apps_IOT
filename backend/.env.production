# ════════════════════════════════════════════════════════════
# MeteoIoT — Configuration Production (VPS)
# ════════════════════════════════════════════════════════════

# Backend
PORT=3002
HOST=0.0.0.0

# MySQL — sur VPS on utilise un MySQL externe (pas embedded)
MYSQL_EMBEDDED=false
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=meteo_user
MYSQL_PASSWORD=CHANGE_ME_STRONG_PASSWORD
MYSQL_DATABASE=meteo_iot

# Simulation (désactiver en production si seuls les capteurs physiques envoient)
# Mettre à 0 pour désactiver
SIMULATION_INTERVAL_MS=10000
